
'use strict';

/**
 * Calculate the age in days based on the given age in years.
 *
 * @param {number} age - The age in years.
 * @returns {number} The age in days.
 */
function calcAgeInDays(age) {
    return age * 365;
}

// Example usage:
console.log(calcAgeInDays(24)); // Output: 8760
